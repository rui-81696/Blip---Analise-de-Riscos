{
  "name": "bet-generator-server",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "start": "node index.js",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "cleanup:demo:dry": "node db/cleanup-demo.js --dry-run",
    "cleanup:demo:delete": "node db/cleanup-demo.js --delete"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.1.0",
    "pg": "^8.20.0",
    "ws": "^8.18.0"
  }
}
